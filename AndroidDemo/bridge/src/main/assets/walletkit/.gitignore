# Vite build artifacts - generated by `pnpm build` in apps/androidkit
# These files should NOT be committed as they are build outputs

# Vite metadata
.vite/

# Compiled JavaScript bundles from Vite build
assets/*.js
assets/*.js.map

# QuickJS compiled bundle (generated by vite.config.quickjs.ts)
quickjs/index.js

# HTML files (generated)
*.html

# Keep directory structure
!assets/.gitkeep
!quickjs/.gitkeep
